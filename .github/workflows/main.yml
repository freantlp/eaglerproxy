name: Run EaglerProxy
on: [workflow_dispatch]

jobs:
  run-proxy:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
      - name: Build and Launch from Folder
        run: |
          cd replit_runtime # This moves GitHub into your folder
          npm install
          npx tsc || true
          echo "--- PROXY STARTING NOW ---"
          node build/index.js
