name: Run EaglerProxy
on: [workflow_dispatch]

jobs:
  run-proxy:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true # This addresses the warning
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
      - name: Build and Launch
        run: |
          cd replit_runtime
          npm install
          npx tsc || true
          echo "--- PROXY ATTEMPTING TO CONNECT TO ATERNOS ---"
          node build/index.js
